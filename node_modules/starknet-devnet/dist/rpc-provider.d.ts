import { AxiosInstance } from "axios";
export declare class RpcProvider {
    private httpProvider;
    private url;
    constructor(httpProvider: AxiosInstance, url: string);
    sendRequest(method: string, params?: unknown): Promise<any>;
}
